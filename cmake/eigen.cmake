#-------------------------------------------------
# eigen
#-------------------------------------------------

set(EIGEN ${OPT_DIR}/eigen)

include_directories(${EIGEN})

message(STATUS "EIGEN=${EIGEN}")
