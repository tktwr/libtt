#-------------------------------------------------
# cmdline
#-------------------------------------------------

set(CMDLINE ${OPT_DIR}/cmdline)

include_directories(${CMDLINE})

message(STATUS "CMDLINE=${CMDLINE}")
